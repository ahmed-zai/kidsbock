const getDashboardData = (req, res) => {
    // Placeholder for dashboard data logic
    res.status(200).json({ message: 'Dashboard data accessed successfully!' });
};

module.exports = {
    getDashboardData,
};
